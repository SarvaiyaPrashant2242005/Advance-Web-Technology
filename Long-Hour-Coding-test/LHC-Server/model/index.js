const Prescription = require('./prescription');
const Patient = require('./patient');
const Checkup = require('./checkupform'); // if this is what you mean by checkup
const Payment = require('./Payment');

module.exports = {
  Prescription,
  Patient,
  Checkup,
  Payment
};
